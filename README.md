## Common lisp library for manipulating android .dex (and related) files

can currently read .dex files and android binary xml file, and
assemble/disassemble dalvik bytecode. (APIs and asm syntax may change
for a while though).


